package com.palle;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication  // Used to start the spring boot Application
public class SbEcommerceProjectApplication {

	public static void main(String[] args) {
		SpringApplication.run(SbEcommerceProjectApplication.class, args);
	}

}

// 1. Contoller -> Service -> Repository -> Database
